{
  "id": "flow-05",
  "title": "Else and Elif",
  "category": "Control Flow",
  "number": 32,
  "duration": "7 min read",
  "content": "## Beyond Simple If\n\nOften you need to handle multiple conditions or provide an alternative when a condition is false. Python provides `else` and `elif` for these cases.\n\n## The else Clause\n\nThe `else` block executes when the `if` condition is false:\n\n```python\nage = 15\n\nif age >= 18:\n    print(\"You are an adult\")\nelse:\n    print(\"You are a minor\")\n```\n\n### else Guarantees One Path Executes\n\n```python\ntemperature = 25\n\nif temperature > 30:\n    print(\"Hot day!\")\nelse:\n    print(\"Not too hot\")  # This will print\n\n# Exactly one of the blocks will always execute\n```\n\n## The elif Clause\n\n`elif` (short for \"else if\") lets you check multiple conditions:\n\n```python\nscore = 85\n\nif score >= 90:\n    grade = \"A\"\nelif score >= 80:\n    grade = \"B\"\nelif score >= 70:\n    grade = \"C\"\nelif score >= 60:\n    grade = \"D\"\nelse:\n    grade = \"F\"\n\nprint(f\"Your grade: {grade}\")  # B\n```\n\n### How elif Works\n\n- Conditions are checked in order from top to bottom\n- The first true condition's block executes\n- Once a condition matches, the rest are skipped\n- The `else` is optional and acts as a \"catch-all\"\n\n```python\nx = 5\n\nif x > 10:\n    print(\"Greater than 10\")\nelif x > 5:\n    print(\"Greater than 5\")\nelif x > 0:          # This matches!\n    print(\"Positive\")  # This prints\nelif x > -5:         # Skipped, even though true\n    print(\"Greater than -5\")\nelse:                # Also skipped\n    print(\"Very negative\")\n```\n\n## if vs elif Difference\n\nUsing multiple `if` statements is different from using `elif`:\n\n```python\n# Multiple if - ALL conditions checked independently\nx = 15\nif x > 10:\n    print(\"A\")  # Prints\nif x > 5:\n    print(\"B\")  # Prints (checked independently)\nif x > 0:\n    print(\"C\")  # Prints (checked independently)\n# Output: A, B, C\n\n# Using elif - only FIRST match executes\nx = 15\nif x > 10:\n    print(\"A\")  # Prints\nelif x > 5:\n    print(\"B\")  # Skipped\nelif x > 0:\n    print(\"C\")  # Skipped\n# Output: A\n```\n\n## Practical Examples\n\n### Menu Selection\n\n```python\nchoice = input(\"Enter choice (1-3): \")\n\nif choice == \"1\":\n    print(\"Starting new game...\")\nelif choice == \"2\":\n    print(\"Loading saved game...\")\nelif choice == \"3\":\n    print(\"Exiting...\")\nelse:\n    print(\"Invalid choice!\")\n```\n\n### Day of Week\n\n```python\nday = 3\n\nif day == 1:\n    name = \"Monday\"\nelif day == 2:\n    name = \"Tuesday\"\nelif day == 3:\n    name = \"Wednesday\"\nelif day == 4:\n    name = \"Thursday\"\nelif day == 5:\n    name = \"Friday\"\nelif day in (6, 7):\n    name = \"Weekend\"\nelse:\n    name = \"Invalid day\"\n\nprint(f\"Today is {name}\")\n```\n\n### Temperature Ranges\n\n```python\ntemp = 72\n\nif temp >= 90:\n    feel = \"Hot!\"\nelif temp >= 70:\n    feel = \"Warm and pleasant\"\nelif temp >= 50:\n    feel = \"Cool\"\nelif temp >= 32:\n    feel = \"Cold\"\nelse:\n    feel = \"Freezing!\"\n\nprint(f\"{temp}°F feels {feel}\")\n```\n\n### Login Status\n\n```python\nusername = \"admin\"\npassword = \"secret\"\n\nif username == \"\" or password == \"\":\n    print(\"Please fill in all fields\")\nelif username == \"admin\" and password == \"secret\":\n    print(\"Welcome, administrator!\")\nelif username == \"admin\":\n    print(\"Wrong password\")\nelse:\n    print(\"User not found\")\n```\n\n## Ternary Conditional Expression\n\nPython has a one-line conditional expression:\n\n```python\n# Syntax: value_if_true if condition else value_if_false\n\nage = 20\nstatus = \"adult\" if age >= 18 else \"minor\"\nprint(status)  # \"adult\"\n\n# Equivalent to:\nif age >= 18:\n    status = \"adult\"\nelse:\n    status = \"minor\"\n```\n\n### Ternary Use Cases\n\n```python\n# Setting default values\nname = user_input if user_input else \"Guest\"\n\n# Conditional formatting\nprint(f\"You have {count} item{'s' if count != 1 else ''}\")\n\n# Quick calculations\ndiscount = 0.2 if is_member else 0\n```\n\n## Common Patterns\n\n### Early Return with else\n\n```python\ndef get_grade(score):\n    if score < 0 or score > 100:\n        return \"Invalid score\"\n    elif score >= 90:\n        return \"A\"\n    elif score >= 80:\n        return \"B\"\n    else:\n        return \"C or below\"\n```\n\n### Validation Chain\n\n```python\ndata = input(\"Enter a number: \")\n\nif not data:\n    print(\"No input provided\")\nelif not data.isdigit():\n    print(\"Not a valid number\")\nelif int(data) < 0:\n    print(\"Number must be positive\")\nelse:\n    print(f\"Valid number: {data}\")\n```\n\n## Summary\n\n- `else` provides an alternative when `if` is false\n- `elif` allows checking multiple conditions in sequence\n- Only the first matching condition's block executes\n- Multiple `if` statements check all conditions independently\n- Use ternary expressions for simple one-line conditionals\n- Order matters: put more specific conditions first",
  "code": "# Else and Elif in Python\n\n# Simple if-else\nprint(\"--- If-Else ---\")\nage = 15\n\nif age >= 18:\n    print(\"Adult\")\nelse:\n    print(\"Minor\")\n\n# Multiple conditions with elif\nprint(\"\\n--- Grade Calculator ---\")\nscore = 78\n\nif score >= 90:\n    grade = \"A\"\nelif score >= 80:\n    grade = \"B\"\nelif score >= 70:\n    grade = \"C\"\nelif score >= 60:\n    grade = \"D\"\nelse:\n    grade = \"F\"\n\nprint(f\"Score: {score} -> Grade: {grade}\")\n\n# Multiple conditions example\nprint(\"\\n--- Temperature Check ---\")\ntemp = 65\n\nif temp >= 85:\n    print(\"Hot!\")\nelif temp >= 70:\n    print(\"Warm\")\nelif temp >= 55:\n    print(\"Cool\")\nelse:\n    print(\"Cold!\")\n\n# Ternary expression\nprint(\"\\n--- Ternary Expression ---\")\nx = 10\nresult = \"positive\" if x > 0 else \"non-positive\"\nprint(f\"{x} is {result}\")\n\n# Practical: Menu selection\nprint(\"\\n--- Menu Demo ---\")\nchoice = \"2\"\n\nif choice == \"1\":\n    print(\"New Game\")\nelif choice == \"2\":\n    print(\"Load Game\")\nelif choice == \"3\":\n    print(\"Settings\")\nelse:\n    print(\"Invalid option\")"
}
